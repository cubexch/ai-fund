{
  "name": "@ai-fund/robinhood",
  "version": "0.1.0",
  "description": "Robinhood connector for AI Hedge Fund — crypto only (official API)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run test:unit",
    "test:unit": "vitest run",
    "test:integration": "vitest run --passWithNoTests \"tests/**/*.integration.test.ts\"",
    "test:platform": "vitest run --passWithNoTests \"tests/**/*.platform.test.ts\"",
    "test:live": "vitest run --passWithNoTests \"tests/**/*.live.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "login": "tsx src/cli/login.ts",
    "logout": "tsx src/cli/logout.ts",
    "status": "tsx src/cli/status.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ai-fund/lib": "*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
