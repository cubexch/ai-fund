{
  "name": "@ai-fund/ccxt",
  "version": "0.1.0",
  "description": "Universal CCXT connector for AI Hedge Fund — trade on Coinbase, Binance, Bybit, and 100+ exchanges",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run test:unit",
    "test:unit": "vitest run --config vitest.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:platform": "vitest run --passWithNoTests \"tests/**/*.platform.test.ts\"",
    "test:live": "vitest run --passWithNoTests \"tests/**/*.live.test.ts\"",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "status": "tsx src/cli/status.ts",
    "login": "tsx src/cli/login.ts",
    "logout": "tsx src/cli/logout.ts"
  },
  "dependencies": {
    "@ai-fund/lib": "*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ccxt": "^4.4.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.2"
  }
}
